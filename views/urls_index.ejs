<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>All URLS | TinyApp</title>
</head>
<body style="font-size:20px; padding:50px;">
  <% include ./partials/_header %>
  <h2>Shortened URLs</h2>
  <% for (var shortURL in urls) { %>
      <ul>
        <li><b><%= shortURL %></b> will go to: <%= urls[shortURL] %>
          <a href="/urls/<%= shortURL %>"><button type="button">Update</button></a>
          <form method="POST" action="<%= "/urls/"+shortURL+"/delete" %>"><button type="submit">Delete</button></form>
        </li>
      </ul>
  <% } %>
  <h5><a href="/urls/new">Shorten More!</a></h5>
</body>
</html>