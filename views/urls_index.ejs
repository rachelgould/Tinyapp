<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="/styles.css" type="text/css">
  <link href="https://fonts.googleapis.com/css?family=Work+Sans" rel="stylesheet">
</head>
<body style="font-size:20px; padding:50px;">
  <% include ./partials/_header %>
  <h2 class="center">Shortened Links</h2>
  <% if (Object.keys(urls).length === 0) { %>
    <img src="https://media1.tenor.com/images/d79dbb3d2f70aa3b6e488c8c4bbdd36e/tenor.gif" alt="Link dancing" class="center">
    <p class="center">You haven't shortened any Links yet! <a href="/urls/new">Click here</a> to get started.</p>
  <% } else { %>
    <% for (var shortURL in urls) { %>
        <ul>
          <li><b><a href="/u/<%= shortURL %>">/u/<%= shortURL %></a></b> will go to: <%= urls[shortURL]['longURL'] %> <h6>(Link created on <%= urls[shortURL]['createdOn'] %>. Visited <%= urls[shortURL]['traffic'] %> times by <%= calcUniqueVisitors(shortURL) %> people.)</h6>
          <div class = "index-buttons">
            <a href="/urls/<%= shortURL %>"><button type="button">Update</button></a>
            <form method="POST" action="<%= "/urls/"+shortURL+"/delete?_method=DELETE" %>"><button type="submit">Delete</button></form>
          </div>
          </li>
        </ul>
    <% } %>
      <h5><a href="/urls/new">🔗 Shorten More!</a></h5>
  <% } %>
</body>
</html>