<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css" integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <title>All URLS | TinyApp</title>
  <style>
    .buttons {
      display: flex;
    }
    ul {
      list-style-type: none;
      padding-left: 10px;
    }
    img {
      max-width: 500px;
    }
  </style>
</head>
<body style="font-size:20px; padding:50px;">
  <% include ./partials/_header %>
  <h2>Shortened Links</h2>
  <% if (Object.keys(urls).length === 0) { %>
    <img src="https://media1.tenor.com/images/d79dbb3d2f70aa3b6e488c8c4bbdd36e/tenor.gif" alt="Link dancing">
    <h6>You haven't shortened any Links yet! <a href="/urls/new">Click here</a> to get started.</h6>
  <% } else { %>
    <% for (var shortURL in urls) { %>
        <ul>
          <li><b><a href="/u/<%= shortURL %>">/u/<%= shortURL %></a></b> will go to: <%= urls[shortURL]['longURL'] %> <h6>(Link created on <%= urls[shortURL]['createdOn'] %>. Visited <%= urls[shortURL]['traffic'] %> times by <%= calcUniqueVisitors(shortURL) %> people.)</h6>
          <div class = "buttons">
            <a href="/urls/<%= shortURL %>"><button type="button" class="btn btn-primary">Update</button></a>
            <form method="POST" action="<%= "/urls/"+shortURL+"/delete?_method=DELETE" %>" class="form-inline"><button type="submit" class="btn btn-primary">🗑️</button></form>
          </div>
          </li>
        </ul>
    <% } %>
      <h5><a href="/urls/new">🔗 Shorten More!</a></h5>
  <% } %>
</body>
</html>